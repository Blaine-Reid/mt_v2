/**
 * @prettier
 */

import React from 'react';
// import { string } from 'prop-types';
import Section from '../Section/Section';

const Hero = ({}) => {
  return (
    <Section
      classNames="flex container flex-row center hero"
      backgroundImg="bg"
    ></Section>
  );
};

Hero.propTypes = {};

export default Hero;
